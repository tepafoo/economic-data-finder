/**
 * Constructor.
 * 
 * @returns
 */
function Constants() {
}

Constants.dbVersion = '1.2';

// the values in Constants.applicationSettings are used as key in the local storage. They can therefore not be numbers.
Constants.applicationSettings = {
	pageScan : 'pageScan',
	listOfLocationCodes : 'listOfLocationCodes',
	highlightKeywords : 'highlightKeywords',
	clickOnKeywords : 'clickOnKeywords',
	showIndicator : 'showIndicator'
};

// the values in Constants.pageScanSettings in the local storage. Makes more sense to store them as strings.
Constants.pageScanSettings = {
	scanAll : 'scanAll',
	scanManual : 'scanManual',
	defaultSetting : 'scanManual'
};

// list of location codes (country, aggregate)
Constants.listOfLocationCodesSettings = {
	defaultList : '[{"locationType":"1","code":"1W"}]',
	worldBankUrlLocationCodesSeparator : '-'
};

Constants.highlightSettings = {
	defaultSetting : true
};

Constants.clickOnKeywordsSettings = {
	defaultSetting : true
};

Constants.popups = {
	indicators : 0,
	languagesList : 1
};

Constants.indicatorSelectorQueryParams = {
	keywordId : 'keywordId',
	languageCode : 'languageCode',
	indicatorLinkObject : 'indicatorLinkObject'
};

// list of actions supported through messaging
Constants.messaging = {
	actions : {
		getKeywords : 0,
		saveIndicatorsForKeywords : 1,
		getIindicatorsForKeyword : 2,
		notifyNoKeywordsFound : 3,
		scanPage : 4
	},
	detectedLanguage : 'detectedLanguage'
};

Constants.sourceWorldBank = 'world_bank';